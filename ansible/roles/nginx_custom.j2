server {
  listen 80 default;
  listen [::]:80 default;

  listen 443 default;
  listen [::]:443 default;
  
  index index.html index.htm;

  server_name _;

  location / {
    proxy_pass  http://localhost:3000;
  }
}